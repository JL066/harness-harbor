[pytest]
pythonpath = .
markers =
    shared: portable contract, required on both macOS and Windows CI
    macos: macOS-only automated check, runnable on the development Mac
    windows_ci: Windows launcher/platform check runnable on a hosted Windows runner
    windows_manual: requires an operator's real Windows installation and explicit opt-in
